{% extends 'layout.html.twig' %}

{% block title %} menu admin {% endblock %}

{% block content %}
<h2>Menus</h2>
<section>
                {% for menu in menus %}
                    <div class="card mb-3 col-sm-12">
                        <div class="row no-gutters">
                            <div class=col-sm-4">
                                <img class="card-img-top" src="https://via.placeholder.com/250" alt="Card image cap">
                            </div>
                            <div class="col-sm-8">
                                <div class="card-body">
                                    <h5 class="card-title"> {{ AdminMenu.name }}</h5>
                                    <p class="card-text">{{ AdminMenu.description }}</p>
                                    <a href="#" class="btn btn-primary">ajouter au menus</a>
                                </div>
                            </div>
                        </div>
                    </div>
                   <form action="" method="post">
        
                    <input type="text" name="titre" value="">
        
                    <input type="submit" name="submit" value="ajouter" />
                    </form>
                {% endfor %}
            </div>
            </div>
            <hr/>
        </section>
{%  endblock %}
